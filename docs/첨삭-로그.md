# 첨삭 반영 로그

첨삭(피드백)이 올 때마다 날짜·문서·요지를 여기에 기록한다. 원본 파일은 `_guideline/`(커밋 제외)에 보관.

| 날짜 | 문서 | 반영 요지 | 반영 위치 |
|---|---|---|---|
| 2026-07-08 | 강의계획서 | 표 중심 실무형으로 축소: 섹션 개명(강의 개요·강좌 목적), 핵심이해·커리큘럼맵·인지수준 삭제, 학습목표 3~4개(학습자 관점), 출석 감점 학칙 규정 추가, 주교재 표(저자-출판사-교재명-발행년도), 주별계획=주차/주제만, 과제 표 신설 | `skills/syllabus/SKILL.md`, `skills/check-syllabus/SKILL.md` |
| 2026-07-28 | 교재(1주차) | 구조는 유지, 내용 깊이·사실 정확성 보강: 개념 정의+예시로 그치지 말 것, AECT 1994·2004 정의 정확 인용·5영역=설계/개발/활용/관리/평가(ADDIE와 구분), 역사 시기·대표 이론가 필수 커버, 균형 서술(구성주의 유일 지향 금지), '체제'·'공학'(갤브레이스) 등 개념 정밀, 사례=본문 모형과 명칭 일치 | `skills/textbook/SKILL.md` |
| 2026-07-28 | PPT/슬라이드(1~15주차) | 렌더러: 순서 연결자 다이아몬드→화살표, 표·비교·카드·도형 텍스트 가운데 정렬, 비순차 번호 제거, 도형 색 구분, 표 포맷(가운데·헤더 bold·행 적으면 축소), 세로형 사진 크롭 제거, 강조 칩은 문제/질문/목표/퀴즈만. 프롬프트: 고정 오프닝(S1 내용 타이틀/S2 개요/S3 목표 2~3/S4-5 HOOK/퀴즈 1문제1슬라이드 정답 우하단/마무리=요약·예고·과제·참고문헌), 제목 간결, 명사형 어미, 교수자 나레이션 삭제, 학습목표 슬라이드 이미지 없음·사례/이론/인물 이미지 지정, 중복 병합 | `core/deck_builder.py`, `skills/slides/SKILL.md` |
| 2026-08-03 | PPT/슬라이드 — 디자인 시스템 | 한글이 지정 폰트로 렌더되지 않던 문제(`a:latin` 만 세팅) → `a:ea`·`a:cs` 동시 세팅 + Pretendard `.pptx` 임베드. 무지개 5색 로테이션 → 네이비 단일 계조 + 앰버 1점. 한글 행간 1.15→1.42. 텍스트 오버플로 보호 신설(`_fit`/`_fit_each`/`_trim_to_fit`). 일정 시각 반경(`_adj`)·사진·카드 그림자·러닝 푸터·페이지 번호. 표 기본 파랑 밴딩 제거. 사진 프리셋 순환 키를 절대 인덱스→사진 순번. 신규 레이아웃 5종(퀴즈 정답 우하단·학습목표·목차·마무리·수치) | `core/deck_builder.py`, `core/pptx_font.py`, `skills/slides/SKILL.md`, `docs/디자인-토큰.md` |
| 2026-08-03 | PPT/슬라이드 — 사진 위치 | 옆단 사진의 윗선을 본문 시작선(2.95")→**요약칩 윗선(1.88")** 에 맞추고 콘텐츠 하단(6.57")까지. 칩 옆이 비어 위쪽이 헐거워 보였다. 높이는 고정하고 폭만 바꿔 변화를 준다 | `core/deck_builder.py` `PHOTO_PRESETS` |
| 2026-08-04 | 앱 구조 | Streamlit 폐기 → FastAPI + 정적 프런트. "목록은 패널(위층), 작업은 바탕(아래층)" 2층 라우터, 좌측 레일(주차 목록·진행 뱃지), 워크스페이스 패널(강좌·강의정보·연결설정·슬라이드양식) | `server.py`, `static/` |
| 2026-08-04 | 슬라이드 파이프라인 | 한 버튼이 아트디렉터+사진검색+빌드+프롬프트를 다 하던 것을 **5단계로 분해**(개요/초안 PPT/비주얼 정돈/씬 프롬프트/이미지 합치기). 주차 폴더도 단계와 1:1(`00_교재`~`05_합치기`, 하위 `assets/`). 합치기가 자동 수집 사진을 잃던 버그 수정(자동 사진 위에 내 이미지를 덮음) | `server.py`, `core/workspace.py`, `static/js/week-ppt.js` |
| 2026-08-04 | 구성적 정렬 | 아트디렉터 단계에서 이론이 끊기던 문제: 플랜 스키마에 `level`(Bloom 6수준) 추가 + **인지수준→레이아웃 정렬 규칙**(분석·평가면 비교·표, 기억이면 카드·표 등). 개요에 `지지 목표: (인지수준) 목표N` 줄 신설. level 은 슬라이드에 인쇄하지 않고(학습자용 화면에 설계 태그 노출 금지) 교수자 점검용으로 UI 에 분포만 표시. 씬 프롬프트 JSON 에도 실어 비디오 단계의 나레이션 속도 근거로 넘김 | `core/deck_builder.py`, `skills/slides/SKILL.md`, `server.py` |
| 2026-08-04 | 폴백 파서 | 아트디렉터 청크 실패 시 도는 블록 파서가 `시각자료 제안`·`강조 신호`·**`발표자 노트`** 같은 설계 메타 줄을 슬라이드 불릿으로 새게 하던 버그 수정(나레이션 문단이 통째로 불릿이 됐다). '본문 개요' 하위 불릿만 본문으로 삼고 다음 라벨 줄에서 멈춘다 | `core/deck_builder.py` `_block_to_bullets` |

## 앞으로
- 하루 1건씩 첨삭이 계속 들어옴(여러 강의 완성 시까지). 산출물 재첨삭도 가능.
- 새 첨삭 → 해당 `skills/*/SKILL.md` 편집 + 위 표에 한 줄 추가 + 앱 재시작.

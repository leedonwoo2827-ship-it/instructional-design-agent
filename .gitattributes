# .bat 은 CP949 + CRLF 다. git 이 줄바꿈을 건드리면 cmd.exe 가 인수를 못 읽어
# 서버가 안 뜬다(전례 있음). 바이트를 그대로 보존한다.
*.bat -text

# 나머지는 작업 트리에서 CRLF, 저장소에는 LF
* text=auto
*.pptx binary
*.png binary
*.jpg binary
*.mp4 binary
*.wav binary

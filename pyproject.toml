[project]
name = "instructional-design-agent"
version = "0.1.0"
description = "교수설계 가이드 에이전트 — 강의계획서/교재/슬라이드 설계 (FastAPI 로컬 콘솔, Ubion LiteLLM proxy)"
requires-python = ">=3.10,<3.14"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "openai>=1.50",
  "python-dotenv>=1.0",
  "markdown>=3.6",
  "python-pptx>=0.6",
  "requests>=2.31",
  "pillow>=10",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["core*"]
